# UML Diagrams

PlantUML source files live here:
- `domain.puml`: core entities and relations (class diagram)
- `components.puml`: API/services/data stores (component diagram)
- `sequence_publish.puml`: draft -> scheduled -> published flow (sequence)
- `sequence_citation_snapshot.puml`: citation + snapshot ingestion flow (sequence)

Render options:
- VS Code PlantUML extension
- `plantuml` CLI (if installed)

